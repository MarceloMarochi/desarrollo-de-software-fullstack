/* Recibimos tres parametros, comparamos y devolvemos de mayor a menor o al reves*/
// También tiene que tener un cuarto parametro para saber si devolvemos de forma ascendente o descendente

let ordenar = (a,b,c,x) => {
    if (a > b  && a > c) {
        mayor = a
    } if (b > c) {
        medio = b
    } else {
        medio = c
    } else if () { 
        if (b > a  && b > c) {
        mayor = b
    } if (a > c) {
        medio = a
    } else {
        medio = c
    }
    }
}